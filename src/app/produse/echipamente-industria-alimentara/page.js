import CategoryPlaceholderPage from '../../products/components/CategoryPlaceholderPage.js';

export const metadata = {
  robots: {
    index: false,
    follow: true,
  },
};

export default function EchipamenteIndustriaAlimentaraPage() {
  return <CategoryPlaceholderPage categoryId="food-industry-equipment" locale="ro" />;
}
